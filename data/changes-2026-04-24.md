# Änderungen 2026-04-24

## 1. QuickWins vereinfacht (dashboard-react/src/components/dashboard/QuickWins.tsx)
- Alte aufgeblähte Karte mit #1/#2/#3 + Score-Verbesserung entfernt
- Ersetzt durch kompakten Info-Balken: "X schnell behebbare Probleme — Geschätzter Aufwand: ca. Y Minuten"
- Nur Clock-Icon + Text, kein eigener Kartenblock mehr

## 2. OptimizationBanner auf Hauptseite (dashboard-react/src/app/page.tsx)
- `OptimizationBanner` importiert und zwischen DomainHeroSection und Content-Grid eingebunden
- Zeigt KI-Optimierungen-Status (verfügbar/verbraucht) + Expertenservice-CTA

## 3. AGB-Check smarter (backend/compliance_engine/checks/agb_check.py)
- `_is_shop()` Funktion: erkennt Shop-Signale (Warenkorb, Checkout, Zahlung etc.)
- Nicht-Shop-Seiten bekommen bei fehlenden AGB nur `severity='info'` + `risk_euro=0` + Hinweis "nicht verpflichtend"
- Shop-Seiten bekommen weiterhin `severity='warning'` + `risk_euro=2000`
- `check_agb_compliance()` akzeptiert optionalen `is_shop` Parameter

## 4. KI-Fix DB-Fehler behoben (backend/main_production.py)
- `create_fix_job`: Scan-Lookup blockiert nicht mehr (graceful fallback)
- user_id wird als int gecastet für asyncpg-Kompatibilität
- Timestamp-Konvertierung mit try/except abgesichert

## 5. KI-Fix über alle Bereiche (dashboard-react/src/components/dashboard/ComplianceIssueCard.tsx)
- `canOptimize = true` — Fix-Button immer verfügbar, unabhängig vom Lock-Status
- Harte Blockade durch Warning-Box entfernt
- Nur sanfter Hinweis wenn andere Seite gelockt ist (kein Block)
- `planType === 'free'` Hinweis ohne `canOptimize`-Bedingung
