# Barrierefreiheits-Analyse Report

**Generiert am:** 23.11.2025, 15:15:43

---

## 📊 Übersicht

| Metrik | Wert |
|--------|------|
| Analysierte Dateien | 17 |
| Mit `<main>` Element | 7 (41%) |
| Mit `<h1>` Überschrift | 12 (71%) |
| Mit `<header>` Element | 3 |
| Mit `<nav>` Element | 0 |
| Mit `<section>` Element | 7 |
| Mit `<article>` Element | 1 |
| Mit `<aside>` Element | 1 |
| Mit `<footer>` Element | 0 |

### Compliance Status

❌ **Verbesserungsbedarf** - 56% Compliance

---

## 📋 Detaillierte Analyse

### ❌ `src/app/ai-compliance/layout.tsx`

**Status:** ⚠️ 3 Problem(e) gefunden

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🔴 **Fehlende <h1> Überschrift**
   - Fügen Sie eine <h1> Überschrift hinzu, die das Hauptthema der Seite beschreibt.

3. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ✅ `src/app/ai-compliance/page.tsx`

**Status:** ✅ Alle Barrierefreiheits-Checks bestanden

**Gefundene semantische Elemente:**

- `<main>` in Zeile 69
- `<main>` in Zeile 85
- `<header>` in Zeile 88
- `<section>` in Zeile 116
- `<article>` in Zeile 118
- `<article>` in Zeile 129
- `<article>` in Zeile 140
- `<article>` in Zeile 151
- `<section>` in Zeile 166
- `<section>` in Zeile 201

**H1-Überschrift:** ✅ Gefunden in Zeile 90

---

### ❌ `src/app/ai-compliance/systems/[id]/page.tsx`

**Status:** ⚠️ 2 Problem(e) gefunden

**H1-Überschrift:** ✅ Gefunden in Zeile 97

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ❌ `src/app/ai-compliance/systems/new/page.tsx`

**Status:** ⚠️ 2 Problem(e) gefunden

**H1-Überschrift:** ✅ Gefunden in Zeile 110

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ❌ `src/app/ai-compliance/upgrade/page.tsx`

**Status:** ⚠️ 2 Problem(e) gefunden

**H1-Überschrift:** ✅ Gefunden in Zeile 71

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ❌ `src/app/auth/callback/page.tsx`

**Status:** ⚠️ 3 Problem(e) gefunden

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🔴 **Fehlende <h1> Überschrift**
   - Fügen Sie eine <h1> Überschrift hinzu, die das Hauptthema der Seite beschreibt.

3. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ✅ `src/app/cookie-compliance/page.tsx`

**Status:** ✅ Alle Barrierefreiheits-Checks bestanden

**Gefundene semantische Elemente:**

- `<main>` in Zeile 123
- `<main>` in Zeile 133
- `<header>` in Zeile 135
- `<section>` in Zeile 170

**H1-Überschrift:** ✅ Gefunden in Zeile 154

---

### ❌ `src/app/error.tsx`

**Status:** ⚠️ 2 Problem(e) gefunden

**H1-Überschrift:** ✅ Gefunden in Zeile 12

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ❌ `src/app/journey/page.tsx`

**Status:** ⚠️ 2 Problem(e) gefunden

**H1-Überschrift:** ✅ Gefunden in Zeile 82

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ❌ `src/app/layout.tsx`

**Status:** ⚠️ 3 Problem(e) gefunden

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🔴 **Fehlende <h1> Überschrift**
   - Fügen Sie eine <h1> Überschrift hinzu, die das Hauptthema der Seite beschreibt.

3. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ✅ `src/app/login/page.tsx`

**Status:** ✅ Alle Barrierefreiheits-Checks bestanden

**Gefundene semantische Elemente:**

- `<main>` in Zeile 85
- `<section>` in Zeile 93

**H1-Überschrift:** ✅ Gefunden in Zeile 100

---

### ❌ `src/app/not-found.tsx`

**Status:** ⚠️ 2 Problem(e) gefunden

**H1-Überschrift:** ✅ Gefunden in Zeile 4

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ❌ `src/app/page.tsx`

**Status:** ⚠️ 1 Problem(e) gefunden

**Gefundene semantische Elemente:**

- `<main>` in Zeile 49
- `<section>` in Zeile 51
- `<section>` in Zeile 56
- `<aside>` in Zeile 59
- `<section>` in Zeile 68
- `<section>` in Zeile 75
- `<section>` in Zeile 82

**Probleme:**

1. 🔴 **Fehlende <h1> Überschrift**
   - Fügen Sie eine <h1> Überschrift hinzu, die das Hauptthema der Seite beschreibt.

---

### ✅ `src/app/profile/page.tsx`

**Status:** ✅ Alle Barrierefreiheits-Checks bestanden

**Gefundene semantische Elemente:**

- `<main>` in Zeile 92
- `<header>` in Zeile 94
- `<section>` in Zeile 105

**H1-Überschrift:** ✅ Gefunden in Zeile 98

---

### ❌ `src/app/providers.tsx`

**Status:** ⚠️ 3 Problem(e) gefunden

**Probleme:**

1. 🔴 **Fehlendes <main> Element**
   - Fügen Sie ein <main> Element hinzu, um den Hauptinhalt der Seite zu kennzeichnen.

2. 🔴 **Fehlende <h1> Überschrift**
   - Fügen Sie eine <h1> Überschrift hinzu, die das Hauptthema der Seite beschreibt.

3. 🟡 **Keine semantischen Strukturelemente gefunden**
   - Verwenden Sie semantische HTML5-Elemente wie <header>, <nav>, <section>, <article> für bessere Barrierefreiheit.

---

### ✅ `src/app/register/page.tsx`

**Status:** ✅ Alle Barrierefreiheits-Checks bestanden

**Gefundene semantische Elemente:**

- `<main>` in Zeile 65
- `<section>` in Zeile 66
- `<main>` in Zeile 197

**H1-Überschrift:** ✅ Gefunden in Zeile 72

---

### ✅ `src/app/subscription/page.tsx`

**Status:** ✅ Alle Barrierefreiheits-Checks bestanden

**Gefundene semantische Elemente:**

- `<main>` in Zeile 50
- `<main>` in Zeile 54
- `<main>` in Zeile 58
- `<section>` in Zeile 59

**H1-Überschrift:** ✅ Gefunden in Zeile 60

---

## 💡 Empfehlungen

### Allgemeine Best Practices

1. **`<main>` Element:** Jede Seite sollte genau ein `<main>` Element haben, das den Hauptinhalt umschließt.

2. **H1-Überschrift:** Jede Seite sollte genau eine `<h1>` Überschrift haben, die das Hauptthema beschreibt.

3. **Semantische Struktur:**
   - `<header>`: Für Kopfbereiche mit Logo, Navigation
   - `<nav>`: Für Navigationsbereiche
   - `<section>`: Für thematische Gruppierungen
   - `<article>`: Für eigenständige, wiederverwendbare Inhalte
   - `<aside>`: Für ergänzende Inhalte (Sidebars)
   - `<footer>`: Für Fußbereiche

4. **ARIA-Attribute:** Verwenden Sie `role` und `aria-label` Attribute für zusätzliche Kontext-Informationen.

### Beispiel-Struktur

```tsx
export default function Page() {
  return (
    <main role="main" aria-label="Hauptinhalt">
      <header>
        <h1>Seitentitel</h1>
        <nav aria-label="Hauptnavigation">
          {/* Navigation Items */}
        </nav>
      </header>

      <section aria-label="Inhaltsbereich">
        <article>
          {/* Hauptinhalt */}
        </article>
      </section>

      <aside aria-label="Zusätzliche Informationen">
        {/* Sidebar */}
      </aside>

      <footer>
        {/* Footer Inhalt */}
      </footer>
    </main>
  );
}
```

---

**Tool:** Accessibility Analyzer v1.0
**Projekt:** Complyo Dashboard
